{
  "name": "@bkwld/lightkeeper",
  "version": "0.0.3",
  "description": "Averages multiple successive Lighthouse tests to arrive at a more accurate PageSpeed score",
  "repository": "git@github.com:BKWLD/lightkeeper.git",
  "author": "info@bukwild.com",
  "license": "MIT",
  "scripts": {
    "analyze": "coffee analyze.coffee",
    "build": "coffee --compile --bare --no-header analyze.coffee",
    "version": "npm run build && git add -A",
    "postversion": "git push --follow-tags"
  },
  "bin": {
    "lightkeeper": "analyze.js"
  },
  "dependencies": {
    "@caporal/core": "^2.0.2",
    "chalk": "^4.1.0",
    "cli-table": "^0.3.1",
    "lighthouse": "^6.3.0",
    "progress-estimator": "^0.3.0"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1"
  }
}
